import "@mono/config-tailwind/global.css"; // shared css variables and config
import ReactDOM from "react-dom/client";
import App from "./App.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(<App />)
